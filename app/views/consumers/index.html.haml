%h1 Listing consumers

%table
  %thead
    %tr
      %th Fullname
      %th Passportnumber
      %th Identitycode
      %th House
      %th Housesection
      %th Apartment
      %th
      %th
      %th

  %tbody
    - @consumers.each do |consumer|
      %tr
        %td= consumer.fullName
        %td= consumer.passportNumber
        %td= consumer.identityCode
        %td= consumer.house
        %td= consumer.houseSection
        %td= consumer.apartment
        %td= link_to 'Show', consumer
        %td= link_to 'Edit', edit_consumer_path(consumer)
        %td= link_to 'Destroy', consumer, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Consumer', new_consumer_path
