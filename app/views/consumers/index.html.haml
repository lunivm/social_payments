%h1 Listing consumers

%table
  %thead
    %tr
      %th Full name
      %th Passport number
      %th Identity code
      %th Street
      %th House
      %th House section
      %th Apartment
      %th
      %th
      %th

  %tbody
    - @consumers.each do |consumer|
      %tr
        %td= consumer.full_name
        %td= consumer.passport_number
        %td= consumer.identity_code
        %td= consumer.street.name
        %td= consumer.house
        %td= consumer.house_section
        %td= consumer.apartment
        %td= link_to 'Show', consumer
        %td= link_to 'Edit', edit_consumer_path(consumer)
        %td= link_to 'Destroy', consumer, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Consumer', new_consumer_path
